# 后端调用指南

## ✅ 是的，你的 app 现在可以计算相关功能了！

### 📋 前提条件

1. **后端服务已启动** ✅
   ```bash
   cd backend
   python main.py
   # 或
   uvicorn main:app --host 0.0.0.0 --port 5000
   ```

2. **网络权限已配置** ✅
   - 已在 `module.json5` 中配置
   - 包括 `ohos.permission.INTERNET`

3. **使用新的 API 服务** ✅
   - 已创建 `BackendAPIService.ts`
   - 提供了完整的调用示例

## 🚀 如何使用

### 步骤 1：配置服务器地址

编辑 `entry/src/main/ets/services/BackendAPIService.ts`：

```typescript
const BACKEND_CONFIG = {
  // 修改为你的实际服务器地址
  baseURL: 'http://192.168.1.100:5000',  // 替换为你的 IP
  timeout: 30000
};
```

### 步骤 2：在页面中使用

#### 示例 1：计算多项式根

```typescript
import { backendAPI } from '../services/BackendAPIService';
import { Logger } from '../utils/Logger';

async onClick() {
  // 计算 x^2 - 5x + 6 = 0 的根
  const response = await backendAPI.findPolynomialRoots({
    coefficients: [6, -5, 1],
    variable: 'x'
  });
  
  if (response.success && response.data) {
    Logger.info('找到根:', response.data.roots);
    // 根: [{real: 3, imag: 0}, {real: 2, imag: 0}]
  }
}
```

#### 示例 2：复数运算

```typescript
async onClick() {
  // 计算 (3+4i) * (1-2i)
  const response = await backendAPI.complexOperation({
    real1: 3,
    imag1: 4,
    real2: 1,
    imag2: -2,
    operation: 'multiply'
  });
  
  if (response.success && response.data) {
    Logger.info('结果:', `${response.data.real} + ${response.data.imag}i`);
    // 结果: 11 + -2i
  }
}
```

#### 示例 3：求解微分方程

```typescript
async onClick() {
  // 求解 y'' + 2y' + y = 0
  const response = await backendAPI.solveODE({
    equationType: 'second_order_constant',
    parameters: { a: 2, b: 1 },
    initialCondition: { y0: 1, dy0: 0 },
    timeRange: [0, 10]
  });
  
  if (response.success && response.data) {
    Logger.info('通解:', response.data.general_solution);
  }
}
```

## 📱 完整使用示例

### 在 CalculatorPage 中添加后端支持

修改 `entry/src/main/ets/pages/CalculatorPage.ets`:

```typescript
import { backendAPI } from '../services/BackendAPIService';

@Entry
@Component
struct CalculatorPage {
  @State expression: string = '';
  @State result: string = '';
  @State showAdvanced: boolean = false;
  
  // 使用后端计算
  async calculateWithBackend() {
    try {
      // 健康检查
      const health = await backendAPI.healthCheck();
      if (!health.success) {
        this.result = '后端服务不可用，使用本地计算';
        return;
      }
      
      // 简化表达式
      const response = await backendAPI.simplify(this.expression);
      if (response.success && response.data) {
        this.result = response.data.result || '';
      }
      
    } catch (error) {
      this.result = '计算错误';
    }
  }
  
  // 快速多项式求根
  async findRootsButton() {
    const response = await backendAPI.findPolynomialRoots({
      coefficients: [6, -5, 1],
      variable: 'x'
    });
    
    if (response.success && response.data) {
      this.result = `根: ${JSON.stringify(response.data.roots)}`;
    }
  }
  
  build() {
    Column() {
      // 表达式输入
      TextInput({ text: this.expression, placeholder: '输入表达式' })
        .onChange((value: string) => { this.expression = value; })
      
      // 计算按钮
      Button('使用后端计算')
        .onClick(() => { this.calculateWithBackend(); })
      
      // 求根按钮
      Button('多项式求根')
        .onClick(() => { this.findRootsButton(); })
      
      // 结果显示
      Text(this.result)
        .fontSize(18)
    }
  }
}
```

## 🔧 配置说明

### 本地开发（同一台机器）

```typescript
const BACKEND_CONFIG = {
  baseURL: 'http://localhost:5000'
};
```

### 移动设备访问电脑

1. 查看电脑 IP（Mac）：
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```

2. 更新配置：
   ```typescript
   const BACKEND_CONFIG = {
     baseURL: 'http://192.168.1.100:5000'  // 替换为你的 IP
   };
   ```

3. 修改后端启动命令：
   ```bash
   # 允许外部访问
   python main.py
   # 或
   uvicorn main:app --host 0.0.0.0 --port 5000
   ```

## 📊 功能对比

### 前端（本地）

- ✅ 快速响应
- ✅ 离线可用
- ✅ 基础计算功能
- ⚠️ 复杂计算受限

### 后端（服务端）

- ✅ **完整** SymPy 功能
- ✅ 高精度计算
- ✅ 复杂计算支持
- ⚠️ 需要网络
- ⚠️ 可能有延迟

### 推荐策略

- **简单计算** → 使用前端
- **复杂计算** → 使用后端
- **离线使用** → 使用前端
- **高精度需求** → 使用后端

## 🎯 快速测试

### 测试后端连接

在你的页面中添加：

```typescript
async mounted() {
  // 测试后端连接
  const health = await backendAPI.healthCheck();
  if (health.success) {
    Logger.info('✅ 后端服务正常');
  } else {
    Logger.warn('⚠️ 后端服务不可用');
  }
}
```

### 测试功能

```typescript
// 测试多项式求根
const testResponse = await backendAPI.findPolynomialRoots({
  coefficients: [6, -5, 1],
  variable: 'x'
});
console.log('测试结果:', testResponse);
```

## 📝 完整的功能列表

后端提供以下计算功能：

1. ✅ **多项式运算**
   - 求值、求根、求导

2. ✅ **复数运算**
   - 加减乘除、幂运算、开方、模长、幅角

3. ✅ **微分方程**
   - 二阶常系数、一阶数值解

4. ✅ **函数优化**
   - BFGS、黄金分割、Newton 方法

5. ✅ **插值**
   - Lagrange、三次样条

6. ✅ **数值求根**
   - Newton-Raphson、二分法

7. ✅ **符号计算**
   - 求导、积分、化简、展开、因式分解

## ✨ 总结

**是的，你的 app 现在完全可以使用后端服务了！**

只需要：
1. ✅ 启动后端服务
2. ✅ 配置服务器地址
3. ✅ 在代码中调用 `backendAPI`

详细示例代码在：
- `entry/src/main/ets/services/BackendAPIService.ts` - API 服务
- `entry/src/main/ets/examples/BackendAPIUsageExample.ts` - 使用示例

