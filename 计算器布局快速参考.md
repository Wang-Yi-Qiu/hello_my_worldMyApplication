# 计算器布局快速参考

## 📐 按钮尺寸规格

### 基础模式
- **按钮尺寸**: 56px × 22%宽
- **圆角**: 28px
- **字体**: 18-24px
- **行高**: 64px
- **间距**: 8px

### 科学模式
- **按钮尺寸**: 52px × 23%宽
- **圆角**: 24px
- **字体**: 14-20px
- **行高**: 52px
- **间距**: 8px

### 程序员模式
- **按钮尺寸**: 52px × 15%宽
- **圆角**: 24px
- **字体**: 12-18px
- **行高**: 52px
- **间距**: 8px

## 🎨 显示区域规格

### 表达式显示
- **字体大小**: 26px（基础）/ 20px（程序员）
- **字体粗细**: Medium
- **颜色**: PRIMARY_COLOR
- **最大行数**: 2行

### 结果显示
- **字体大小**: 64px（基础）/ 52px（程序员）
- **字体粗细**: Bold
- **颜色**: SUCCESS_COLOR / ERROR_COLOR
- **字间距**: 2px

### 区域尺寸
- **高度**: 220px（基础）/ 280px（程序员）
- **内边距**: 24px（上下）/ 24px（左右）

## 🔢 按钮布局矩阵

### 基础模式（5×4 = 20个按钮）
```
┌────┬────┬────┬────┐
│ AC │ ⌫  │ %  │ ÷  │
├────┼────┼────┼────┤
│ 7  │ 8  │ 9  │ ×  │
├────┼────┼────┼────┤
│ 4  │ 5  │ 6  │ -  │
├────┼────┼────┼────┤
│ 1  │ 2  │ 3  │ +  │
├────┼────┼────┼────┤
│+/- │ 0  │ .  │ =  │
└────┴────┴────┴────┘
```

### 科学模式（8×4 = 32个按钮）
```
┌────┬────┬────┬────┐
│sin │cos │tan │ ln │
├────┼────┼────┼────┤
│ √  │ ^  │ (  │ )  │
├────┼────┼────┼────┤
│ AC │ ⌫  │ %  │ ÷  │
├────┼────┼────┼────┤
│ 7  │ 8  │ 9  │ ×  │
├────┼────┼────┼────┤
│ 4  │ 5  │ 6  │ -  │
├────┼────┼────┼────┤
│ 1  │ 2  │ 3  │ +  │
├────┼────┼────┼────┤
│+/- │ 0  │ .  │ =  │
├────┼────┼────┼────┤
│ π  │ e  │log │ x² │
└────┴────┴────┴────┘
```

### 程序员模式（6×6 = 30个按钮 + 6个空位）
```
┌────┬────┬────┬────┬────┬────┐
│LSH │RSH │ C  │ (  │ )  │ AC │
├────┼────┼────┼────┼────┼────┤
│ OR │ A  │ B  │ 7  │ 8  │ 9  │
├────┼────┼────┼────┼────┼────┤
│XOR │ C  │ D  │ 4  │ 5  │ 6  │
├────┼────┼────┼────┼────┼────┤
│NOT │ E  │ F  │ 1  │ 2  │ 3  │
├────┼────┼────┼────┼────┼────┤
│AND │ ×  │ ÷  │ 0  │ .  │ =  │
├────┼────┼────┼────┼────┼────┤
│ +  │ -  │ %  │ ⌫  │    │    │
└────┴────┴────┴────┴────┴────┘
```

## 🎯 按钮类型与颜色

### 按钮类型
- **number**: 数字按钮（0-9）
- **operator**: 运算符（+、-、×、÷）
- **function**: 功能按钮（⌫、%、(、)、√、^等）
- **clear**: 清除按钮（AC、C）
- **equals**: 等号按钮（=）
- **constant**: 常数按钮（π、e）
- **hex**: 十六进制按钮（A-F）
- **bitwise**: 位运算按钮（AND、OR、XOR、NOT、LSH、RSH）

### 颜色方案
- **operator**: PRIMARY_COLOR（蓝色）
- **equals**: SUCCESS_COLOR（绿色）
- **clear/bitwise**: ERROR_COLOR（红色）
- **其他**: CARD_BG_DARK / DIVIDER_COLOR_LIGHT（灰色）

## 📱 响应式设计

### 宽度计算
- **基础模式**: `22% × 4 + 2% × 3 = 94%`（留6%边距）
- **科学模式**: `23% × 4 + 1% × 3 = 95%`（留5%边距）
- **程序员模式**: `15% × 6 + 1% × 5 = 95%`（留5%边距）

### 对齐方式
- **水平**: `FlexAlign.SpaceBetween`
- **垂直**: `FlexAlign.End`（显示区域）

## 🔧 关键方法

### 按钮构建
```typescript
buildRoundButton(label, type, widthMultiplier)     // 基础模式
buildScientificButton(label, type, widthPercent)   // 科学模式
buildProgrammerButton(label, type, widthPercent)   // 程序员模式
```

### 样式获取
```typescript
getButtonFontSize(label)           // 获取字体大小
getButtonColor(type)               // 获取背景色
getButtonTextColor(type)           // 获取文字颜色
getButtonWidth(widthMultiplier)    // 获取按钮宽度
```

### 事件处理
```typescript
handleButtonClick(value)           // 按钮点击处理
calculateResult()                  // 计算表达式
```

## 📊 性能优化

### 渲染优化
- 使用 `@Builder` 装饰器复用组件
- `ForEach` 遍历按钮数组
- 统一的样式计算方法

### 布局优化
- 百分比宽度，避免固定像素
- `FlexAlign.SpaceBetween` 自动分布
- 统一的行高和间距

## 🧪 测试检查清单

- [ ] 所有按钮大小一致（同一模式内）
- [ ] 按钮间距均匀
- [ ] 矩形布局完美对齐
- [ ] 显示区域字体清晰
- [ ] 长表达式正常显示（2行）
- [ ] 所有按钮点击响应正常
- [ ] 深色/浅色主题显示正常
- [ ] 不同屏幕尺寸适配良好

## 📝 维护注意事项

1. **添加新按钮**: 确保保持矩形布局，必要时调整列数
2. **修改尺寸**: 同步修改按钮高度、圆角、间距
3. **调整字体**: 使用 `getButtonFontSize()` 统一管理
4. **更改颜色**: 通过 `getButtonColor()` 集中控制
5. **布局调整**: 注意宽度百分比总和不超过100%

## 🔗 相关文件

- **主文件**: `entry/src/main/ets/pages/CalculatorPage.ets`
- **主题配置**: `entry/src/main/ets/utils/AppTheme.ts`
- **表达式引擎**: `entry/src/main/ets/modules/ExpressionEngine.ts`
- **详细文档**: `计算器布局优化总结.md`

