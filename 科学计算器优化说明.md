# 科学计算器优化说明

## 优化内容

### 1. 布局重新设计
- **从5列改为4列布局**：使按钮更大，更易于点击
- **优化按钮大小**：
  - 科学模式：64x64 像素
  - 基础模式：76x76 像素
- **调整按钮间距**：科学模式行间距从8px减少到4px，节省空间
- **优化显示区域**：科学模式显示区域从180px减少到140px，为按钮留出更多空间

### 2. 按钮布局优化
采用更符合使用习惯的布局：

```
第1行: AC    ⌫     (     )
第2行: sin   cos   tan   ÷
第3行: ln    7     8     9
第4行: x²    4     5     6
第5行: √     1     2     3
第6行: π     e     0     .
第7行: xʸ    ×     -     +
第8行: log   %     +/-   =
```

**设计理念**：
- 常用数字集中在中间区域（3x3网格）
- 运算符在右侧列，方便右手操作
- 函数按钮在左侧列，逻辑分组
- 清除和括号在顶部，便于快速访问

### 3. 字体大小优化
- 长文本（sin, cos, tan, log）：16px
- 两字符（AC, ln, xʸ, x²）：18px
- 单字符（数字和运算符）：24px

### 4. 功能完善
所有按钮功能都已实现：

#### 三角函数
- `sin(x)` - 正弦（角度制）
- `cos(x)` - 余弦（角度制）
- `tan(x)` - 正切（角度制）

#### 对数函数
- `ln(x)` - 自然对数（以e为底）
- `log(x)` - 常用对数（以10为底）

#### 幂和根
- `x²` - 平方
- `xʸ` - 幂运算（输入 ^ 符号）
- `√` - 平方根

#### 常数
- `π` - 圆周率（3.14159...）
- `e` - 自然常数（2.71828...）

#### 其他
- `%` - 百分号（除以100）
- `+/-` - 正负号切换
- `!` - 阶乘（暂未实现，显示提示）

### 5. 计算引擎支持
所有函数都由 `ExpressionEngine.ts` 支持：
- 三角函数自动进行角度到弧度的转换
- 反三角函数返回角度值
- 支持复杂表达式嵌套
- 高精度计算

## 使用示例

### 基础计算
```
输入: 2+3×4
结果: 14
```

### 三角函数
```
输入: sin(30)
结果: 0.5
```

### 复杂表达式
```
输入: (sin(45)+cos(45))×π
结果: 4.44288...
```

### 幂运算
```
输入: 2^10
结果: 1024
```

### 对数
```
输入: ln(e)
结果: 1

输入: log(100)
结果: 2
```

## 用户体验改进

1. **更舒适的按钮大小**：按钮从50px增加到64px，更易点击
2. **更清晰的布局**：4列布局比5列更宽敞
3. **更合理的分组**：数字、函数、运算符分区明确
4. **更大的字体**：从14-20px增加到16-24px，更易阅读
5. **更好的视觉反馈**：保持了原有的颜色方案和圆角设计

## 技术细节

### 响应式设计
- 按钮宽度：22%（4列布局，留出间距）
- 按钮高度：根据模式动态调整
- 行高：根据模式动态调整（基础80px，科学68px）

### 性能优化
- 删除了不再使用的 `buildScientificButtonRow` 方法
- 统一使用 `buildButtonRow` 方法，减少代码重复
- 优化了字体大小计算逻辑

## 下一步计划

1. 实现阶乘功能（!）
2. 添加更多科学函数（如 asin, acos, atan）
3. 支持角度/弧度切换
4. 添加历史记录功能
5. 支持表达式编辑和光标移动

