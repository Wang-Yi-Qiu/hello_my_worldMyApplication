# 数学计算引擎集成指南

## 概述

本文档介绍如何在 HarmonyOS 应用中集成强大的数学计算引擎，参考了以下开源项目：

1. **SageMath** - https://github.com/sagemath/sage
2. **MathFunctionTool** - https://github.com/chenwenhang/MathFunctionTool

## 当前实现

### 1. 本地计算引擎 (CalculusEngine)

**位置：** `entry/src/main/ets/modules/CalculusEngine.ts`

**功能：**
- ✅ 数值积分（辛普森法则）
- ✅ 符号求导（基础实现）
- ✅ 极限计算（数值方法 + 标准极限识别）
- ✅ 表达式求值（安全的 Function 构造）

**优点：**
- 无需网络连接
- 响应速度快
- 隐私保护好

**限制：**
- 仅支持基础的符号计算
- 复杂表达式可能无法处理
- 精度受限于数值方法

**使用示例：**

```typescript
import { CalculusEngine } from '../modules/CalculusEngine';

const engine = new CalculusEngine();

// 定积分
const result = engine.definiteIntegral('sin(x)', 'x', 0, Math.PI);
console.log(result.value); // ≈ 2.0

// 求导
const derivative = engine.symbolicDerivative('x^2', 'x');
console.log(derivative.derivative); // "2x"

// 极限
const limit = engine.limit('sin(x)/x', 'x', 0);
console.log(limit.limit); // "1"
```

### 2. SageMath 集成服务

**位置：** `entry/src/main/ets/services/SageMathService.ts`

**功能：**
- ✅ 通过 HTTP API 调用 SageMath
- ✅ 支持符号积分
- ✅ 支持复杂的数学运算
- ✅ 精确的符号计算

**集成方式：**

#### 方式一：使用 SageMathCell 公共服务

```typescript
import { sageMathService } from '../services/SageMathService';

// 计算定积分
const result = await sageMathService.computeDefiniteIntegral(
  'sin(x)',
  'x',
  '0',
  'pi'
);

console.log(result.result); // "2"
```

**优点：**
- 无需自建服务器
- 功能强大，支持所有 SageMath 功能

**缺点：**
- 需要网络连接
- 响应速度较慢（网络延迟）
- 依赖第三方服务

#### 方式二：自建 SageMath 服务器

**步骤：**

1. **安装 SageMath**

```bash
# Ubuntu/Debian
sudo apt-get install sagemath

# macOS
brew install sage

# 或使用 Docker
docker pull sagemath/sagemath
```

2. **创建 API 服务器**

```python
# sage_server.py
from flask import Flask, request, jsonify
from sage.all import *

app = Flask(__name__)

@app.route('/compute', methods=['POST'])
def compute():
    try:
        code = request.json['code']
        result = eval(code)
        return jsonify({
            'success': True,
            'result': str(result)
        })
    except Exception as e:
        return jsonify({
            'success': False,
            'error': str(e)
        })

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=8080)
```

3. **运行服务器**

```bash
python sage_server.py
```

4. **配置应用**

```typescript
import { sageMathService } from '../services/SageMathService';

// 设置自定义服务器
sageMathService.setCustomServer('http://your-server:8080/compute');

// 使用
const result = await sageMathService.execute({
  code: `
from sage.all import *
x = var('x')
integrate(sin(x), (x, 0, pi))
  `
});
```

## 参考项目分析

### MathFunctionTool 项目

**GitHub:** https://github.com/chenwenhang/MathFunctionTool

**关键特性：**

1. **表达式解析**
   - 使用 `exp4j` 库进行表达式解析
   - 支持自定义函数和运算符
   - 错误处理和验证

2. **数值计算**
   - 数值积分（梯形法则、辛普森法则）
   - 数值求导（差分法）
   - 方程求解（牛顿法、二分法）

3. **绘图功能**
   - 使用 MPAndroidChart 绘制函数图像
   - 支持缩放和平移
   - 多函数对比

**可借鉴的实现：**

```java
// 表达式求值（参考）
public class ExpressionEvaluator {
    private Expression expression;
    
    public double evaluate(double x) {
        expression.setVariable("x", x);
        return expression.evaluate();
    }
    
    // 数值积分
    public double integrate(double a, double b, int n) {
        double h = (b - a) / n;
        double sum = 0;
        
        for (int i = 0; i < n; i++) {
            double x1 = a + i * h;
            double x2 = a + (i + 1) * h;
            sum += (evaluate(x1) + evaluate(x2)) * h / 2;
        }
        
        return sum;
    }
}
```

**在 HarmonyOS 中的对应实现：**

```typescript
// 已实现在 CalculusEngine.ts
export class CalculusEngine {
  // 数值积分（辛普森法则）
  numericalIntegral(f: (x: number) => number, a: number, b: number, n: number = 1000): number {
    if (n % 2 !== 0) n++;
    const h = (b - a) / n;
    let sum = f(a) + f(b);
    
    for (let i = 1; i < n; i++) {
      const x = a + i * h;
      sum += (i % 2 === 0 ? 2 : 4) * f(x);
    }
    
    return (h / 3) * sum;
  }
}
```

### SageMath 项目

**GitHub:** https://github.com/sagemath/sage

**关键特性：**

1. **符号计算**
   - 基于 SymPy
   - 精确的符号积分
   - 符号求导、极限

2. **数值计算**
   - 高精度数值计算
   - 特殊函数
   - 线性代数

3. **API 接口**
   - SageMathCell API
   - Jupyter Kernel
   - REST API

**集成示例：**

```python
# SageMath 代码示例
from sage.all import *

# 定义变量
x = var('x')

# 符号积分
result = integrate(sin(x), (x, 0, pi))
print(result)  # 输出: 2

# 不定积分
result = integrate(x^2, x)
print(result)  # 输出: x^3/3

# 极限
result = limit(sin(x)/x, x=0)
print(result)  # 输出: 1

# 求导
result = diff(sin(x), x)
print(result)  # 输出: cos(x)
```

**在 HarmonyOS 中的调用：**

```typescript
import { sageMathService } from '../services/SageMathService';

// 定积分
const integral = await sageMathService.computeDefiniteIntegral(
  'sin(x)',
  'x',
  '0',
  'pi'
);

// 不定积分
const indefinite = await sageMathService.computeIndefiniteIntegral(
  'x^2',
  'x'
);

// 导数
const derivative = await sageMathService.computeDerivative(
  'sin(x)',
  'x'
);

// 极限
const limit = await sageMathService.computeLimit(
  'sin(x)/x',
  'x',
  '0'
);
```

## 混合策略（推荐）

结合本地计算和远程 API 的优势：

```typescript
export class HybridMathEngine {
  private localEngine: CalculusEngine;
  private remoteService: SageMathService;
  
  constructor() {
    this.localEngine = new CalculusEngine();
    this.remoteService = new SageMathService();
  }
  
  /**
   * 智能积分计算
   * 优先使用本地引擎，复杂情况回退到 SageMath
   */
  async computeIntegral(
    expression: string,
    variable: string,
    from?: number,
    to?: number
  ): Promise<IntegralResult> {
    
    // 1. 尝试本地计算
    if (from !== undefined && to !== undefined) {
      const localResult = this.localEngine.definiteIntegral(
        expression,
        variable,
        from,
        to
      );
      
      if (localResult.success) {
        return localResult;
      }
    }
    
    // 2. 本地失败，尝试 SageMath
    try {
      if (from !== undefined && to !== undefined) {
        const remoteResult = await this.remoteService.computeDefiniteIntegral(
          expression,
          variable,
          from.toString(),
          to.toString()
        );
        
        if (remoteResult.success) {
          return {
            success: true,
            value: parseFloat(remoteResult.result || '0'),
            steps: [`使用 SageMath 计算: ${remoteResult.result}`]
          };
        }
      }
    } catch (error) {
      Logger.warn('SageMath calculation failed, using local result');
    }
    
    // 3. 返回本地结果（即使有错误）
    return this.localEngine.definiteIntegral(expression, variable, from!, to!);
  }
}
```

## 性能对比

| 方法 | 响应时间 | 精度 | 功能范围 | 网络依赖 |
|------|----------|------|----------|----------|
| 本地引擎 | < 100ms | 中等 | 基础 | 无 |
| SageMathCell | 1-5s | 高 | 完整 | 是 |
| 自建服务器 | 200-500ms | 高 | 完整 | 是（局域网） |

## 最佳实践

1. **优先使用本地引擎**
   - 对于简单的数值计算
   - 需要快速响应的场景
   - 离线使用

2. **使用 SageMath API**
   - 复杂的符号计算
   - 需要精确结果
   - 用户明确要求高精度

3. **混合策略**
   - 先尝试本地计算
   - 失败或不满意时调用 API
   - 缓存常用结果

4. **用户体验**
   - 显示计算进度
   - 提供计算方法选择
   - 离线时自动降级

## 未来改进

1. **集成 math.js**
   - 更强大的表达式解析
   - 更多数学函数
   - 单位转换

2. **WebAssembly**
   - 将 SymPy 编译为 WASM
   - 在浏览器中运行
   - 无需服务器

3. **机器学习**
   - 表达式简化
   - 积分方法选择
   - 结果验证

4. **缓存系统**
   - 缓存常用积分结果
   - 离线数据库
   - 云同步

## 总结

通过结合本地计算引擎和 SageMath API，我们可以提供：
- ✅ 快速的基础计算
- ✅ 强大的符号计算
- ✅ 良好的用户体验
- ✅ 离线支持

参考 MathFunctionTool 的实现思路和 SageMath 的强大功能，我们的应用可以满足从基础到高级的各种数学计算需求。

