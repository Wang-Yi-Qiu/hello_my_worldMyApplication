# 重新构建应用指南

## 问题已修复 ✅

定积分计算的问题已经修复。修复内容包括：

1. **变量替换逻辑优化** - 使用占位符策略避免重复替换
2. **安全性检查更新** - 允许数学函数名称中的字母

## 重新构建步骤

### 方法 1: 使用 DevEco Studio (推荐)

1. **打开项目**
   - 启动 DevEco Studio
   - 打开项目：`/Users/wangyiqiu/DevEcoStudioProjects/hello_my_worldMyApplication`

2. **清理构建缓存**
   - 点击菜单：`Build` → `Clean Project`
   - 等待清理完成

3. **重新构建**
   - 点击菜单：`Build` → `Rebuild Project`
   - 等待构建完成

4. **运行应用**
   - 点击工具栏的"运行"按钮（绿色三角形）
   - 或按快捷键 `Shift + F10`

### 方法 2: 使用命令行

```bash
# 进入项目目录
cd /Users/wangyiqiu/DevEcoStudioProjects/hello_my_worldMyApplication

# 清理构建缓存
hvigorw clean

# 重新构建
hvigorw assembleHap

# 安装到设备（如果已连接）
hdc install entry/build/default/outputs/default/entry-default-signed.hap
```

## 验证修复

### 测试步骤

1. **打开应用**
   - 启动"高等数学"应用

2. **进入积分计算**
   - 点击"积分"按钮

3. **选择定积分**
   - 点击"定积分"选项

4. **输入测试数据**
   - 被积函数: `sin(x)`
   - 积分变量: `x`
   - 下限: `0`
   - 上限: `π`

5. **点击计算**
   - 点击"计算"按钮

### 预期结果

应该显示：
```
✅ 计算结果

计算从 0 到 π 的定积分，结果约为 2.000000

∫[0, π] sin(x) dx ≈ 2.000000
```

## 如果仍有问题

### 查看日志

1. **打开 HiLog**
   - 在 DevEco Studio 底部找到"HiLog"标签页
   - 点击打开

2. **筛选日志**
   - 在搜索框中输入：`CalculusEngine`
   - 查看相关日志输出

3. **关键日志**
   - 查找包含 `Evaluating expression` 的日志
   - 查找包含 `Expression result` 的日志
   - 查找任何 `ERROR` 级别的日志

### 常见问题

#### 问题 1: 构建失败
**解决方案**:
```bash
# 删除 node_modules 和 lock 文件
rm -rf node_modules
rm -rf oh_modules
rm package-lock.json

# 重新安装依赖
npm install

# 清理并重新构建
hvigorw clean
hvigorw assembleHap
```

#### 问题 2: 应用无法安装
**解决方案**:
```bash
# 卸载旧版本
hdc uninstall com.example.hellomyworldmyapplication

# 重新安装
hdc install entry/build/default/outputs/default/entry-default-signed.hap
```

#### 问题 3: 仍然显示错误
**请提供以下信息**:
1. HiLog 的完整输出（特别是 ERROR 级别的日志）
2. 输入的具体值
3. 显示的错误信息截图
4. 设备型号和 HarmonyOS 版本

## 技术细节

### 修改的文件
- `entry/src/main/ets/modules/CalculusEngine.ts`

### 修改的方法
- `evaluateExpression()` - 第 197-265 行

### 核心改进
1. **占位符策略**: 使用随机生成的占位符避免变量重复替换
2. **正则表达式优化**: 更精确的变量匹配模式
3. **安全性检查更新**: 支持 Math 对象的方法名

### 测试覆盖
- ✅ 三角函数: sin, cos, tan
- ✅ 指数函数: exp
- ✅ 幂运算: x^2, x^3
- ✅ 复合表达式: x*sin(x), cos(x)+x
- ✅ 定积分: ∫[0, π] sin(x) dx

## 联系方式

如果您在重新构建或测试过程中遇到任何问题，请：
1. 查看 HiLog 日志
2. 截图错误信息
3. 记录具体的操作步骤
4. 提供设备和系统信息

祝您使用愉快！ 🎉

