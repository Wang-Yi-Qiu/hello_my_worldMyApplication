# 定积分功能使用指南

## 🎯 快速开始

### 方法一：引导式输入（推荐）

1. 打开 **高等数学** 页面
2. 点击 **引导式输入** 按钮
3. 选择 **定积分 ∫ᵃᵇ**
4. 填写以下信息：
   - **被积函数 f(x)**：例如 `sin(x)`
   - **积分变量**：通常是 `x`
   - **下限 a**：例如 `0`
   - **上限 b**：例如 `π` 或 `pi`
5. 点击 **确认**
6. 选择 **积分** 功能
7. 点击 **计算**

### 方法二：直接输入

1. 打开 **高等数学** 页面
2. 在输入框中输入：`sin(x)[0,π]`
3. 选择 **积分** 功能
4. 点击 **计算**

## 📝 支持的表达式

### 基本函数

| 函数 | 输入格式 | 示例 |
|------|---------|------|
| 三角函数 | `sin(x)`, `cos(x)`, `tan(x)` | `sin(x)` |
| 反三角函数 | `asin(x)`, `acos(x)`, `atan(x)` | `asin(x)` |
| 指数函数 | `exp(x)` | `exp(x)` |
| 对数函数 | `ln(x)`, `log(x)` | `ln(x)` |
| 幂函数 | `x^n` | `x^2`, `x^3` |
| 平方根 | `sqrt(x)` | `sqrt(x)` |

### 复合表达式

| 类型 | 示例 |
|------|------|
| 加法 | `sin(x) + cos(x)` |
| 减法 | `x^2 - 2*x` |
| 乘法 | `x * sin(x)` |
| 除法 | `sin(x) / x` |
| 组合 | `(x^2 + 1) * exp(x)` |

### 常数

| 常数 | 输入格式 |
|------|---------|
| 圆周率 | `π` 或 `pi` |
| 自然对数底 | `e` |
| 无穷大 | `∞` 或 `inf` |

## 📊 示例计算

### 示例 1：基础三角函数

**问题：** 计算 ∫[0, π] sin(x) dx

**步骤：**
1. 函数：`sin(x)`
2. 变量：`x`
3. 下限：`0`
4. 上限：`π`

**结果：** ≈ 2.000000

**数学验证：**
```
∫[0,π] sin(x) dx = [-cos(x)]₀^π
                  = -cos(π) - (-cos(0))
                  = -(-1) - (-1)
                  = 1 + 1
                  = 2 ✅
```

### 示例 2：多项式

**问题：** 计算 ∫[0, 1] x² dx

**步骤：**
1. 函数：`x^2`
2. 变量：`x`
3. 下限：`0`
4. 上限：`1`

**结果：** ≈ 0.333333

**数学验证：**
```
∫[0,1] x² dx = [x³/3]₀¹
              = 1³/3 - 0³/3
              = 1/3
              ≈ 0.333333 ✅
```

### 示例 3：指数函数

**问题：** 计算 ∫[0, 1] eˣ dx

**步骤：**
1. 函数：`exp(x)`
2. 变量：`x`
3. 下限：`0`
4. 上限：`1`

**结果：** ≈ 1.718282

**数学验证：**
```
∫[0,1] eˣ dx = [eˣ]₀¹
              = e¹ - e⁰
              = e - 1
              ≈ 2.718282 - 1
              = 1.718282 ✅
```

### 示例 4：复合函数

**问题：** 计算 ∫[0, π] x·sin(x) dx

**步骤：**
1. 函数：`x*sin(x)`
2. 变量：`x`
3. 下限：`0`
4. 上限：`π`

**结果：** ≈ 3.141593

**数学验证：**
```
∫[0,π] x·sin(x) dx = [sin(x) - x·cos(x)]₀^π
                    = (sin(π) - π·cos(π)) - (sin(0) - 0·cos(0))
                    = (0 - π·(-1)) - (0 - 0)
                    = π
                    ≈ 3.141593 ✅
```

## 🔧 高级功能

### 1. 查看计算步骤

计算完成后，点击 **详细解题步骤** 查看：
- 积分表达式
- 被积函数
- 积分区间
- 计算方法（辛普森法则）
- 最终结果

### 2. 保存到历史记录

计算结果会自动保存到历史记录，可以：
- 在 **历史记录** 页面查看
- 按类型筛选（积分）
- 收藏重要结果
- 重新计算

### 3. 导出结果

- 复制结果文本
- 分享到其他应用
- 保存为图片

## ⚠️ 注意事项

### 1. 表达式格式

- ✅ **正确：** `sin(x)`, `x^2`, `exp(x)`
- ❌ **错误：** `sinx`, `x2`, `e^x`（应该用 `exp(x)`）

### 2. 乘号

- ✅ **推荐：** `2*x`, `x*sin(x)`
- ⚠️ **可能有问题：** `2x`, `xsin(x)`（建议显式写出 `*`）

### 3. 积分限

- ✅ **支持：** 数字（`0`, `1`, `2.5`）
- ✅ **支持：** 常数（`π`, `pi`, `e`）
- ❌ **不支持：** 表达式（`2*π`）（请先计算为数值）

### 4. 精度

- 使用辛普森法则进行数值积分
- 默认 1000 个分段
- 精度约为 6 位小数
- 对于需要更高精度的计算，建议使用 SageMath API

## 🐛 常见问题

### Q1: 提示"函数在积分区间内无法求值"

**原因：**
- 表达式格式错误
- 函数在区间内有奇点
- 变量名不匹配

**解决方法：**
1. 检查表达式格式（参考上面的示例）
2. 确保函数在积分区间内有定义
3. 使用引导式输入，避免格式错误

### Q2: 结果不准确

**原因：**
- 数值积分的固有误差
- 函数在区间内变化剧烈
- 积分区间过大

**解决方法：**
1. 对于简单函数，误差通常很小（< 0.001）
2. 对于复杂函数，可以分段计算
3. 使用 SageMath API 获取精确的符号结果

### Q3: 计算时间过长

**原因：**
- 积分区间过大
- 函数计算复杂

**解决方法：**
1. 缩小积分区间
2. 简化表达式
3. 使用更快的计算方法

### Q4: 无法输入特殊符号

**解决方法：**
1. 点击 **数学键盘** 按钮
2. 使用英文输入：`pi` 代替 `π`，`inf` 代替 `∞`
3. 使用引导式输入

## 📈 性能参考

| 函数复杂度 | 计算时间 | 精度 |
|-----------|---------|------|
| 简单（x², sin(x)） | < 50ms | 6位小数 |
| 中等（x·sin(x)） | < 100ms | 6位小数 |
| 复杂（嵌套函数） | < 200ms | 5-6位小数 |

## 🎓 数学背景

### 辛普森法则

应用使用 **辛普森法则**（Simpson's Rule）进行数值积分：

```
∫[a,b] f(x)dx ≈ (h/3) × [f(x₀) + 4f(x₁) + 2f(x₂) + 4f(x₃) + ... + f(xₙ)]
```

其中：
- `h = (b-a)/n` 是步长
- `n = 1000` 是分段数
- 偶数项系数为 4，奇数项系数为 2

**优点：**
- 精度高（误差 O(h⁴)）
- 速度快
- 适合大多数函数

## 🔗 相关功能

- **不定积分**：计算原函数
- **求导**：计算导数
- **极限**：计算函数极限
- **方程求解**：求解方程

## 📞 获取帮助

如果遇到问题：
1. 查看 **详细解题步骤**
2. 参考本指南的示例
3. 尝试使用引导式输入
4. 检查表达式格式

---

**祝您使用愉快！** 🎉

如有任何问题或建议，欢迎反馈！

