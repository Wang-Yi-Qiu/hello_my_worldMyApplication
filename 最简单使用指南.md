# 🎯 最简洁的本地配置方法

## 方法：2步搞定

### 步骤 1：启动后端（双击运行）

**Windows:**
```
双击运行 → backend/启动服务.bat
```

**Mac/Linux:**
```bash
# 在 backend 目录下
chmod +x 启动服务.sh
./启动服务.sh
```

或者直接：
```bash
cd backend
python main.py
```

### 步骤 2：什么都不用改！

**配置已经是本地的**，你不需要修改任何代码！

```typescript
// BackendAPIService.ts 第 13 行
baseURL: 'http://localhost:5000',  // ✅ 就是这个，不用改
```

---

## 📱 如果手机要访问电脑

### 只需改 1 个地方

1. 查看电脑 IP：
   ```bash
   # Mac
   ifconfig | grep "inet " | grep -v 127.0.0.1
   
   # Windows
   ipconfig
   ```
   
2. 修改 `BackendAPIService.ts` 第 16 行：
   ```typescript
   // 把这行的注释取消，改成你的 IP
   baseURL: 'http://192.168.1.100:5000',  // 替换成你的 IP
   ```

---

## ✅ 验证是否成功

启动后端后，打开浏览器：

```
http://localhost:5000/docs
```

看到 Swagger 文档页面 = ✅ 成功！

---

## 🎉 总结

**本地开发最简单：**
1. ✅ 双击运行 `backend/启动服务.bat`
2. ✅ 代码不用改，直接用 `localhost:5000`
3. ✅ 在 app 中调用 `backendAPI` 即可

**就这么简单！**

