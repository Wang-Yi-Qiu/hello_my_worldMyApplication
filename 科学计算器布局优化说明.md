# 科学计算器布局优化说明

## 修改概述

根据提供的参考图片，对科学计算器的按钮布局进行了全面优化，使其更符合现代计算器的设计规范。

## 主要修改内容

### 1. 布局结构调整

**新布局特点：**
- 左侧区域（75%宽度）：包含数字、功能键和常用操作
- 右侧区域（25%宽度）：运算符按钮，每个按钮高度为普通按钮的两倍

**按钮分布：**

#### 第一行（5个按钮）
- `rad` - 弧度模式指示器
- `√` - 平方根
- `^` - 幂运算
- `(` - 左括号
- `)` - 右括号

#### 第二行（5个按钮）
- `sin` - 正弦函数
- `cos` - 余弦函数
- `tan` - 正切函数
- `log` - 常用对数（以10为底）
- `ln` - 自然对数（以e为底）

#### 第三行（3个按钮）
- `AC` - 全部清除（红色，突出显示）
- `+/-` - 正负号切换
- `%` - 百分号

#### 第四行（3个按钮 + 右侧÷）
- `7`, `8`, `9` - 数字键
- `÷` - 除法（蓝色长按钮，占两行高度）

#### 第五行（3个按钮 + 右侧×）
- `4`, `5`, `6` - 数字键
- `×` - 乘法（蓝色长按钮，占两行高度）

#### 第六行（3个按钮 + 右侧-）
- `1`, `2`, `3` - 数字键
- `-` - 减法（蓝色长按钮，占两行高度）

#### 第七行（2个按钮 + 右侧+）
- `0`, `.` - 数字键和小数点
- `+` - 加法（蓝色长按钮，占两行高度）

#### 右侧最后
- `=` - 等号（蓝色长按钮，占两行高度）

### 2. 颜色方案优化

根据参考图片调整了颜色方案：

- **数字键**：浅灰色背景 (#E8E8E8)，黑色文字
- **功能键**：浅灰色背景 (#E8E8E8)，黑色文字
- **AC键**：红色背景 (#FF6B6B)，白色文字（突出显示）
- **运算符**：蓝色背景 (#4A90E2)，白色文字
- **等号**：蓝色背景 (#4A90E2)，白色文字

### 3. 新增功能

#### 新增按钮处理：
- **rad按钮**：显示当前为弧度模式的提示
- **^按钮**：幂运算符，直接添加到表达式中

#### 新增构建器方法：
- `buildScientificButton()`：构建科学计算器左侧区域的标准按钮
- `buildTallButton()`：构建右侧高度为两倍的运算符按钮

### 4. 尺寸规格

- **标准按钮**：
  - 宽度：18%（左侧区域）
  - 高度：64px
  - 圆角：32px

- **高按钮**（右侧运算符）：
  - 宽度：100%（右侧区域）
  - 高度：144px（68px × 2 + 8px间距）
  - 圆角：24px

- **按钮间距**：8px

### 5. 字体大小

根据按钮标签长度自动调整：
- 长文本（3个字符以上，如 sin, cos, tan）：16px
- 中等文本（2个字符）：18px
- 单字符：24px

## 功能实现

所有按钮功能均已实现：

1. **数字输入**：0-9 和小数点
2. **基本运算**：+, -, ×, ÷
3. **三角函数**：sin, cos, tan（自动添加括号）
4. **对数函数**：ln, log（自动添加括号）
5. **其他函数**：√（平方根）, ^（幂运算）
6. **括号**：( 和 )
7. **特殊操作**：AC（清除）, +/-（正负切换）, %（百分号）
8. **计算**：=（等号，触发计算）

## 技术实现

### 布局结构

使用 HarmonyOS ArkTS 的声明式UI：
```typescript
Row() {
  // 左侧按钮区域（75%）
  Column() {
    // 7行按钮
  }
  .width('75%')
  
  // 右侧运算符区域（25%）
  Column() {
    // 5个高按钮
  }
  .width('25%')
}
```

### 响应式设计

- 使用百分比宽度确保不同屏幕尺寸下的适配
- 按钮间距统一为8px
- 自动调整字体大小以适应按钮标签

## 使用说明

1. 点击"🔬"按钮切换到科学计算器模式
2. 使用数字键输入数字
3. 使用函数键（sin, cos等）会自动添加左括号
4. 使用右侧的运算符按钮进行计算
5. 点击"="按钮查看结果
6. 点击"AC"按钮清除所有内容
7. 点击"← 基础"按钮返回基础计算器模式

## 注意事项

1. 三角函数默认使用弧度模式
2. 所有计算结果会自动保存到历史记录
3. 表达式支持嵌套括号
4. 支持连续计算（结果可作为下一次计算的输入）

## 后续优化建议

1. 添加角度/弧度模式切换功能
2. 添加更多科学函数（如 asin, acos, atan等）
3. 添加常数按钮（π, e）
4. 支持复杂表达式的可视化编辑
5. 添加计算历史记录快速调用功能

## 文件修改清单

- `entry/src/main/ets/pages/CalculatorPage.ets`
  - 重写 `buildScientificButtons()` 方法
  - 新增 `buildScientificButton()` 方法
  - 新增 `buildTallButton()` 方法
  - 更新 `handleButtonClick()` 方法（添加 rad 和 ^ 的处理）
  - 更新 `getButtonColor()` 方法（优化颜色方案）
  - 更新 `getButtonTextColor()` 方法（简化逻辑）
  - 更新 `buildRoundButton()` 方法（支持科学模式）

## 测试建议

1. 测试所有按钮的点击响应
2. 测试三角函数计算（如 sin(45)）
3. 测试对数函数计算（如 ln(2.718)）
4. 测试复杂表达式（如 sin(30) + cos(60)）
5. 测试括号嵌套
6. 测试正负号切换
7. 测试百分号功能
8. 测试清除功能
9. 测试连续计算
10. 测试不同屏幕尺寸下的显示效果

