# 服务端快速开始指南

## 🚀 快速启动（3 步）

### 1. 安装依赖

```bash
cd backend
pip install -r requirements.txt
```

### 2. 启动服务

```bash
python main.py
```

### 3. 访问 API

打开浏览器访问：
- http://localhost:5000/docs （Swagger UI 交互式文档）
- http://localhost:5000/redoc （ReDoc 文档）

## 📝 验证服务是否运行

```bash
# 健康检查
curl http://localhost:5000/health

# 应该返回：
# {"status":"ok","service":"SmartCalc Backend","version":"2.0.0"}
```

## 🧪 测试新功能

### 测试多项式求根

```bash
curl -X POST http://localhost:5000/polynomial/roots \
  -H "Content-Type: application/json" \
  -d '{"coefficients": [6, -5, 1]}'
```

### 测试复数运算

```bash
curl -X POST http://localhost:5000/complex/operations \
  -H "Content-Type: application/json" \
  -d '{
    "real1": 3,
    "imag1": 4,
    "operation": "magnitude"
  }'
```

### 测试微分方程

```bash
curl -X POST http://localhost:5000/ode/solve \
  -H "Content-Type: application/json" \
  -d '{
    "equation_type": "second_order_constant",
    "parameters": {"a": 0, "b": -1},
    "initial_condition": {"y0": 1, "dy0": 0},
    "time_range": [0, 10]
  }'
```

## 🎯 在 HarmonyOS 应用中集成

在你的 HarmonyOS 应用中添加 HTTP 服务：

```typescript
import { http } from '@kit.NetworkKit';

// 调用多项式 API
async function calculatePolynomial() {
  const url = 'http://your-server-ip:5000/polynomial/roots';
  const requestData = {
    coefficients: [1, -5, 6],
    variable: 'x'
  };
  
  try {
    const response = await http.createHttp().request(url, {
      method: http.RequestMethod.POST,
      header: {
        'Content-Type': 'application/json'
      },
      extraData: JSON.stringify(requestData)
    });
    
    const result = await JSON.parse(response.result.toString());
    console.info('Roots:', result.roots);
  } catch (error) {
    console.error('API Error:', error);
  }
}
```

## 📊 所有可用 API

```
基础功能：
✅ POST /execute           - 执行任意 SymPy 代码
✅ POST /integrate          - 定积分
✅ POST /integrate_indefinite - 不定积分
✅ POST /differentiate     - 求导
✅ POST /solve             - 求解方程
✅ POST /simplify          - 化简表达式
✅ POST /expand            - 展开表达式
✅ POST /factor            - 因式分解
✅ POST /limit             - 计算极限
✅ POST /taylor            - 泰勒展开

新增高级功能：
✅ POST /polynomial/evaluate - 多项式求值
✅ POST /polynomial/roots    - 求多项式根
✅ POST /polynomial/derivative - 多项式求导
✅ POST /complex/operations  - 复数运算
✅ POST /ode/solve          - 微分方程
✅ POST /optimize           - 函数优化
✅ POST /interpolate        - 插值
✅ POST /root_finding       - 数值求根

信息：
✅ GET  /api/stats         - API 统计
✅ GET  /health            - 健康检查
```

## 💡 提示

1. **本地测试**: 使用 `localhost:5000`
2. **移动设备访问**: 使用你的电脑 IP 地址
   ```bash
   # 查看 IP
   ifconfig  # macOS/Linux
   ipconfig  # Windows
   ```
3. **修改端口**: 编辑 `main.py` 最后一行

## ❓ 遇到问题？

1. **端口被占用**
   - 修改端口：`uvicorn main:app --port 8000`

2. **依赖安装失败**
   - 使用虚拟环境：
   ```bash
   python -m venv venv
   source venv/bin/activate  # macOS/Linux
   venv\Scripts\activate     # Windows
   pip install -r requirements.txt
   ```

3. **连接错误**
   - 检查防火墙设置
   - 确保网络连接正常

## 🎉 完成！

现在你的服务端已经完全就绪，可以提供强大的数学计算服务了！

