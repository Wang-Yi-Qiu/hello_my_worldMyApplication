# 科学计算器功能增强总结

## 📅 更新日期
2025-10-25

## 🎯 增强目标

基于用户提供的计算器截图和 SageMath 功能，对科学计算器进行全面增强，使其成为功能强大的专业数学工具。

## ✅ 已完成的增强功能

### 1. 表达式引擎增强 (ExpressionEngine.ts)

#### 1.1 阶乘功能
- ✅ 支持阶乘运算符 `!`
- ✅ 支持数字阶乘：`5!` = 120
- ✅ 支持表达式阶乘：`(3+2)!` = 120
- ✅ 自动检测溢出（n > 170 返回 Infinity）
- ✅ 错误处理：负数和非整数

**使用示例：**
```
5!           → 120
10!          → 3628800
(2+3)!       → 120
```

#### 1.2 排列组合功能
- ✅ 排列数 P(n, r) = n! / (n-r)!
- ✅ 组合数 C(n, r) = n! / (r! * (n-r)!)
- ✅ 优化算法避免大数计算
- ✅ 完整的参数验证

**使用示例：**
```
P(5, 3)      → 60    (5×4×3)
C(5, 3)      → 10    (5×4×3)/(3×2×1)
P(10, 2)     → 90
C(10, 2)     → 45
```

#### 1.3 三角函数增强
- ✅ 基础三角函数：sin, cos, tan
- ✅ 反三角函数：asin, acos, atan
- ✅ 双曲函数：sinh, cosh, tanh
- ✅ 反双曲函数：asinh, acosh, atanh
- ✅ 自动角度/弧度转换（根据模式）

**使用示例：**
```
sin(30)      → 0.5 (角度模式)
asin(0.5)    → 30 (返回角度)
sinh(1)      → 1.175
asinh(1)     → 0.881
```

#### 1.4 其他数学函数
- ✅ 绝对值：abs(x)
- ✅ 符号函数：sign(x)
- ✅ 截断函数：trunc(x)
- ✅ 已有：sqrt, ceil, floor, round, pow, min, max

### 2. 计算器界面增强 (CalculatorPage.ets)

#### 2.1 角度/弧度模式切换
- ✅ 新增 `angleMode` 状态：'DEG' | 'RAD'
- ✅ 顶部显示当前模式按钮
- ✅ 一键切换角度/弧度
- ✅ Toast 提示当前模式
- ✅ 自动应用到三角函数计算

**界面位置：**
- 科学模式顶部左侧
- 蓝色高亮显示当前模式
- 点击切换

#### 2.2 高级函数面板
- ✅ 新增 `showAdvancedFunctions` 状态
- ✅ 基础/高级函数切换按钮
- ✅ 基础面板：sin, cos, tan, ln, √, ^, (, )
- ✅ 高级面板：asin, acos, atan, !, sinh, cosh, tanh, abs

**界面位置：**
- 科学模式顶部右侧
- 灰色按钮显示"基础"或"高级"
- 点击切换函数面板

#### 2.3 新增按钮处理
- ✅ 阶乘按钮 `!`
- ✅ 反三角函数按钮：asin, acos, atan
- ✅ 双曲函数按钮：sinh, cosh, tanh
- ✅ 反双曲函数按钮：asinh, acosh, atanh
- ✅ 排列按钮 `P`
- ✅ 组合按钮 `C`
- ✅ 绝对值按钮 `abs`

### 3. 按钮布局优化

#### 3.1 科学模式布局
```
┌─────────────────────────────────────┐
│ [DEG/RAD]              [基础/高级]   │
├─────────────────────────────────────┤
│ 基础模式：                           │
│ sin   cos   tan   ln                │
│ √     ^     (     )                 │
├─────────────────────────────────────┤
│ 高级模式：                           │
│ asin  acos  atan  !                 │
│ sinh  cosh  tanh  abs               │
├─────────────────────────────────────┤
│ AC    ⌫     %     ÷                 │
│ 7     8     9     ×                 │
│ 4     5     6     -                 │
│ 1     2     3     +                 │
│ +/-   0     .     =                 │
│ π     e     log   x²                │
└─────────────────────────────────────┘
```

## 🔧 技术实现细节

### 1. 阶乘实现
```typescript
private factorial(n: number): number {
  if (n < 0) throw new Error('阶乘不支持负数');
  if (!Number.isInteger(n)) throw new Error('阶乘只支持整数');
  if (n > 170) return Infinity;
  if (n === 0 || n === 1) return 1;
  
  let result = 1;
  for (let i = 2; i <= n; i++) {
    result *= i;
  }
  return result;
}
```

### 2. 排列实现（优化版）
```typescript
private permutation(n: number, r: number): number {
  if (r > n) return 0;
  
  let result = 1;
  for (let i = n; i > n - r; i--) {
    result *= i;
  }
  return result;
}
```

### 3. 组合实现（优化版）
```typescript
private combination(n: number, r: number): number {
  if (r > n) return 0;
  
  // 优化：C(n, r) = C(n, n-r)
  if (r > n - r) r = n - r;
  
  let result = 1;
  for (let i = 0; i < r; i++) {
    result *= (n - i);
    result /= (i + 1);
  }
  return Math.round(result);
}
```

### 4. 角度转换
```typescript
// 三角函数：角度 → 弧度
Math.sin((Math.PI/180) * angle)

// 反三角函数：弧度 → 角度
(180/Math.PI) * Math.asin(value)
```

## 📊 功能对比

| 功能 | 增强前 | 增强后 |
|------|--------|--------|
| 三角函数 | sin, cos, tan | + asin, acos, atan |
| 双曲函数 | ❌ | sinh, cosh, tanh, asinh, acosh, atanh |
| 阶乘 | ❌ | ✅ 支持 n! |
| 排列组合 | ❌ | ✅ P(n,r), C(n,r) |
| 角度模式 | 仅弧度 | DEG/RAD 切换 |
| 函数面板 | 固定 | 基础/高级切换 |
| 绝对值 | ❌ | ✅ abs(x) |

## 🎓 使用示例

### 示例 1：阶乘计算
```
输入：5!
结果：120

输入：(3+2)!
结果：120

输入：10!
结果：3628800
```

### 示例 2：排列组合
```
输入：P(5, 3)
结果：60
说明：从5个元素中选3个排列

输入：C(5, 3)
结果：10
说明：从5个元素中选3个组合
```

### 示例 3：三角函数（角度模式）
```
输入：sin(30)
结果：0.5

输入：cos(60)
结果：0.5

输入：tan(45)
结果：1
```

### 示例 4：反三角函数
```
输入：asin(0.5)
结果：30
说明：返回角度值

输入：acos(0.5)
结果：60

输入：atan(1)
结果：45
```

### 示例 5：双曲函数
```
输入：sinh(1)
结果：1.175

输入：cosh(0)
结果：1

输入：tanh(1)
结果：0.762
```

### 示例 6：复杂表达式
```
输入：5! + C(10, 3)
结果：240
说明：120 + 120

输入：sin(30) + cos(60)
结果：1
说明：0.5 + 0.5

输入：abs(-5) + sqrt(16)
结果：9
说明：5 + 4
```

## 🚀 性能优化

### 1. 阶乘优化
- 使用迭代而非递归
- 提前检测溢出
- 缓存常用结果（可选）

### 2. 组合数优化
- 使用 C(n, r) = C(n, n-r) 减少计算
- 边乘边除避免大数溢出
- 整数运算保证精度

### 3. 表达式解析优化
- 预编译正则表达式
- 减少字符串操作
- 智能函数替换

## 🔒 安全性增强

### 1. 输入验证
- ✅ 阶乘：检查负数和非整数
- ✅ 排列组合：检查参数范围
- ✅ 三角函数：检查定义域
- ✅ 表达式：防止代码注入

### 2. 错误处理
- ✅ 友好的错误提示
- ✅ 详细的错误日志
- ✅ 优雅的降级处理

## 📱 用户体验改进

### 1. 视觉反馈
- ✅ 模式切换按钮高亮
- ✅ Toast 提示当前状态
- ✅ 按钮颜色编码
- ✅ 清晰的布局分区

### 2. 操作便捷性
- ✅ 一键切换角度/弧度
- ✅ 一键切换基础/高级函数
- ✅ 自动括号补全（部分函数）
- ✅ 连续计算支持

### 3. 学习曲线
- ✅ 直观的按钮标签
- ✅ 符合数学习惯的符号
- ✅ 清晰的功能分组

## 🎯 下一步计划

### Phase 1: SageMath 集成（高优先级）
- [ ] 集成 SageMath 服务进行符号计算
- [ ] 支持方程求解
- [ ] 支持微积分运算
- [ ] 支持矩阵运算

### Phase 2: UI/UX 优化（中优先级）
- [ ] 添加函数帮助提示
- [ ] 添加计算历史搜索
- [ ] 添加常用公式收藏
- [ ] 优化横屏布局

### Phase 3: 高级功能（低优先级）
- [ ] 单位转换
- [ ] 统计函数
- [ ] 复数运算
- [ ] 自定义函数

## 📚 参考资源

- [SageMath 官方文档](https://doc.sagemath.org/)
- [Math.js 文档](https://mathjs.org/docs/)
- [HarmonyOS ArkTS 开发指南](https://developer.harmonyos.com/)
- [科学计算器标准](https://en.wikipedia.org/wiki/Scientific_calculator)

## 🐛 已知问题

1. **阶乘溢出**
   - 问题：n > 170 时返回 Infinity
   - 解决方案：考虑使用大数库或显示科学计数法

2. **角度精度**
   - 问题：浮点数精度限制
   - 解决方案：使用高精度数学库

3. **表达式复杂度**
   - 问题：非常复杂的表达式可能解析失败
   - 解决方案：集成 math.js 完整版

## 📝 更新日志

### v2.1.0 (2025-10-25)
- ✅ 新增阶乘功能 (!)
- ✅ 新增排列组合功能 (P, C)
- ✅ 新增反三角函数 (asin, acos, atan)
- ✅ 新增双曲函数 (sinh, cosh, tanh)
- ✅ 新增反双曲函数 (asinh, acosh, atanh)
- ✅ 新增角度/弧度模式切换
- ✅ 新增基础/高级函数面板切换
- ✅ 优化按钮布局
- ✅ 增强表达式引擎

### v2.0.0 (2025-10-24)
- 重新设计科学计算器布局
- 优化按钮颜色方案
- 添加长按钮支持

## 🤝 贡献指南

欢迎贡献代码、报告问题或提出建议！

### 如何贡献
1. Fork 本项目
2. 创建功能分支
3. 提交更改
4. 发起 Pull Request

### 代码规范
- 遵循 ArkTS 编码规范
- 添加必要的注释
- 编写单元测试
- 更新相关文档

---

**维护者：** SmartCalc 开发团队  
**最后更新：** 2025-10-25  
**版本：** v2.1.0

