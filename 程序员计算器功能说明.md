# 程序员计算器功能说明

## 功能概述

程序员计算器模式已完成，支持以下功能：

### 1. 数字系统支持

- **十进制 (DEC)** - 默认模式
- **十六进制 (HEX)** - 支持 A-F 字母输入
- **八进制 (OCT)** - 自动转换显示
- **二进制 (BIN)** - 自动转换显示

### 2. 位运算操作符

| 操作符 | 功能 | 示例 |
|--------|------|------|
| LSH | 左移 (<<) | 8 << 2 = 32 |
| RSH | 右移 (>>) | 32 >> 2 = 8 |
| OR | 按位或 (\|) | 12 \| 10 = 14 |
| XOR | 按位异或 (^) | 12 ^ 10 = 6 |
| NOT | 按位取反 (~) | ~5 = -6 |
| AND | 按位与 (&) | 12 & 10 = 8 |

### 3. 基本运算

- 加法 (+)
- 减法 (-)
- 乘法 (×)
- 除法 (÷)
- 取模 (%)
- 括号 ()

### 4. 十六进制输入

支持直接输入十六进制字母 A-F，系统会自动识别并转换：
- 输入：1A + 2B
- 内部转换：0x1A + 0x2B
- 结果：69 (十进制)

## 按钮布局

```
第一行：LSH  RSH  (    )    ⌫    AC
第二行：OR   A    B    7    8    9
第三行：XOR  C    D    4    5    6
第四行：NOT  E    F    1    2    3
第五行：AND  ×    ÷    0    .    +
第六行：=    (空位保持对齐)
```

## 使用示例

### 示例 1: 十六进制加法
```
输入: A + B
结果: 21 (十进制)
显示:
  HEX: 15
  DEC: 21
  OCT: 25
  BIN: 10101
```

### 示例 2: 位运算
```
输入: 12 & 10
结果: 8
解释: 
  12 (1100)
& 10 (1010)
-----------
   8 (1000)
```

### 示例 3: 左移操作
```
输入: 5 << 2
结果: 20
解释: 5 * 2^2 = 20
```

### 示例 4: 复杂表达式
```
输入: (A + B) × 2
结果: 42
解释: (10 + 11) × 2 = 42
```

## 进制切换

点击显示区域的进制标签（HEX/DEC/OCT/BIN）可以切换当前选中的进制：
- 选中的进制会以蓝色加粗显示
- 目前仅用于视觉指示，所有计算都基于十进制

## 技术实现

### 核心方法

1. **handleProgrammerButtonClick(value: string)**
   - 处理程序员模式的按钮点击
   - 将位运算符转换为 JavaScript 操作符

2. **calculateProgrammerResult()**
   - 计算程序员模式表达式
   - 确保结果为整数

3. **evaluateProgrammerExpression(expr: string)**
   - 解析并计算表达式
   - 支持十六进制数字和位运算

4. **convertHexNumbers(expr: string)**
   - 将十六进制数字转换为 0x 格式
   - 正则匹配包含 A-F 的数字序列

### 数字转换方法

- `convertToHex(value: string)` - 转换为十六进制
- `convertToOct(value: string)` - 转换为八进制
- `convertToBin(value: string)` - 转换为二进制

## 注意事项

1. **XOR vs 幂运算**
   - 在程序员模式下，`^` 是 XOR 运算符
   - 如需幂运算，请使用科学计算器模式

2. **整数运算**
   - 所有结果都会向下取整为整数
   - 小数点仅用于输入，结果不保留小数

3. **负数处理**
   - 位运算对负数使用 JavaScript 的二进制补码表示
   - NOT 运算结果可能为负数

4. **表达式验证**
   - 仅允许安全字符：数字、运算符、括号
   - 无效字符会导致计算错误

## 未来改进

1. 支持不同进制的直接输入（如 0xFF, 0b1010）
2. 添加位操作可视化显示
3. 支持字长选择（8位、16位、32位、64位）
4. 添加更多位运算功能（循环移位等）
5. 支持有符号/无符号整数切换

## 测试建议

建议测试以下场景：
1. 基本十六进制运算：A + B, FF - 10
2. 位运算：12 & 10, 5 | 3, 6 ^ 3
3. 移位运算：8 << 2, 32 >> 2
4. 复杂表达式：(A + B) × (C - D)
5. 括号嵌套：((A & B) | C) ^ D
6. 负数运算：~5, -10 & 15

