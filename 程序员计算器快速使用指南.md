# 程序员计算器快速使用指南

## 快速开始

1. 在计算器顶部点击 **"程序员"** 标签切换到程序员模式
2. 输入表达式，支持十六进制字母 A-F
3. 点击 **=** 计算结果
4. 结果会自动显示为 HEX、DEC、OCT、BIN 四种进制

## 常用操作

### 十六进制计算
```
A + B = 21 (十进制)
FF - 10 = 239
1A × 2 = 52
```

### 位运算
```
按位与：12 & 10 = 8
按位或：12 | 10 = 14
按位异或：12 ^ 10 = 6
按位取反：~5 = -6
左移：8 << 2 = 32
右移：32 >> 2 = 8
```

### 混合运算
```
(A + B) × 2 = 42
(12 & 10) | 5 = 13
FF & (AA | 55) = 255
```

## 按钮说明

| 按钮 | 功能 | 说明 |
|------|------|------|
| A-F | 十六进制字母 | 直接输入 |
| LSH | 左移 | 相当于 << |
| RSH | 右移 | 相当于 >> |
| OR | 按位或 | 相当于 \| |
| XOR | 按位异或 | 相当于 ^ |
| NOT | 按位取反 | 相当于 ~ |
| AND | 按位与 | 相当于 & |
| AC | 全部清除 | 清空表达式 |
| ⌫ | 退格 | 删除最后一个字符 |
| = | 等于 | 计算结果 |

## 进制显示

计算结果会同时显示四种进制：

```
输入：A + B
结果显示：
  HEX: 15      ← 十六进制
  DEC: 21      ← 十进制（默认）
  OCT: 25      ← 八进制
  BIN: 10101   ← 二进制
```

点击进制标签（HEX/DEC/OCT/BIN）可以高亮显示当前选中的进制。

## 实用技巧

### 1. 快速进制转换
输入任意十进制数字，结果会自动显示所有进制：
```
输入：255
显示：
  HEX: FF
  DEC: 255
  OCT: 377
  BIN: 11111111
```

### 2. 位掩码操作
```
设置位：value | (1 << n)
清除位：value & ~(1 << n)
切换位：value ^ (1 << n)
检查位：(value >> n) & 1
```

### 3. 常用位运算模式
```
获取低8位：value & FF
获取高8位：(value >> 8) & FF
交换两个数：a ^ b, b ^ a, a ^ b
判断奇偶：value & 1
```

## 注意事项

⚠️ **重要提示**

1. **整数运算**：所有结果都是整数，小数部分会被舍弃
2. **XOR 符号**：在程序员模式下，`^` 是异或运算，不是幂运算
3. **负数显示**：负数使用二进制补码表示
4. **运算优先级**：建议使用括号明确运算顺序

## 常见问题

**Q: 为什么输入 A 显示 10？**
A: A 是十六进制，等于十进制的 10。

**Q: 如何输入二进制数？**
A: 目前只支持十进制和十六进制输入，二进制仅用于显示结果。

**Q: NOT 运算为什么是负数？**
A: JavaScript 使用 32 位有符号整数，~5 = -6 是正确的补码结果。

**Q: 可以输入 0xFF 这样的格式吗？**
A: 目前不支持，请直接输入 FF，系统会自动识别为十六进制。

## 示例练习

试试以下表达式，熟悉程序员计算器：

```
1. A + B + C + D + E + F = ?
2. FF & AA = ?
3. 10 << 3 = ?
4. (12 | 5) & 9 = ?
5. ~0 = ?
6. 1 << 8 = ?
7. (A ^ B) | (C & D) = ?
8. FF >> 4 = ?
```

答案：
```
1. 75 (十进制)
2. 170 (十进制，AA)
3. 80
4. 9
5. -1
6. 256
7. 结果取决于具体计算
8. 15 (十进制，F)
```

## 更多资源

- 位运算教程：了解位运算的基本概念
- 进制转换：学习不同进制之间的转换方法
- 二进制补码：理解负数的二进制表示

---

**提示**：如需进行科学计算或幂运算，请切换到"科学"模式。

